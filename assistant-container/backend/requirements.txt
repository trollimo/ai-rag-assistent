fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.28.0
pydantic>=2.0
chromadb>=0.5.0
fastembed>=0.3.6
mcp>=1.9.1,<2.0.0
pyyaml>=6.0
# Feedback module only. Imported unconditionally but never connected when
# feedback.enabled is false, so a deployment without Postgres still starts.
sqlalchemy[asyncio]>=2.0
psycopg[binary]>=3.1
