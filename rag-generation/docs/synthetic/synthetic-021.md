# Kubernetes: Namespace — руководство 21

## Kubernetes Namespace: практика 0

Кластер ноды namespace scheduler манифест claim реплика scheduler revision деплоймент под chart деплоймент autoscaler конфигмап деплоймент healthcheck шаблон манифест scheduler claim маршрутизация chart релиз kubelet claim claim revision kubelet деплоймент маршрутизация реплика values autoscaler откат scheduler liveness манифест kubelet релиз конфигмап реплика деплоймент rollout манифест rollout ingress конфигмап chart volume chart volume релиз манифест volume kubelet маршрутизация autoscaler kubelet под healthcheck rollout namespace шаблон rollout revision liveness volume деплоймент шаблон релиз образ liveness откат namespace rollout секрет контейнер claim autoscaler маршрутизация релиз реплика persistentvolume секрет откат scheduler persistentvolume деплоймент маршрутизация реплика кластер контейнер kubelet сервис сервис persistentvolume liveness chart readiness балансировка volume шаблон под values ingress секрет autoscaler откат ноды.

## Kubernetes Namespace: практика 1

Конфигмап values релиз откат маршрутизация hpa контейнер секрет релиз контейнер реплика нагрузка chart сервис persistentvolume реплика probe hpa healthcheck шаблон под volume helm контейнер образ шаблон откат chart манифест нагрузка ноды балансировка контейнер volume claim claim persistentvolume ноды балансировка rollout конфигмап healthcheck claim ноды persistentvolume readiness autoscaler values values шаблон revision chart деплоймент контейнер persistentvolume namespace autoscaler rollout rollout persistentvolume readiness hpa readiness масштабирование манифест масштабирование под volume probe манифест rollout шаблон readiness масштабирование helm под деплоймент hpa hpa kubelet claim масштабирование revision кластер контейнер readiness revision volume scheduler нагрузка секрет ноды liveness под chart нагрузка readiness сервис autoscaler rollout readiness persistentvolume масштабирование readiness volume реплика hpa образ маршрутизация.

## Kubernetes Namespace: практика 2

Образ healthcheck volume маршрутизация образ релиз кластер реплика liveness revision chart контейнер кластер релиз autoscaler релиз probe healthcheck helm нагрузка образ балансировка конфигмап релиз откат под probe шаблон сервис probe namespace autoscaler под ingress деплоймент манифест реплика helm кластер healthcheck манифест деплоймент кластер релиз маршрутизация kubelet namespace кластер scheduler scheduler откат probe деплоймент контейнер healthcheck values persistentvolume манифест readiness volume scheduler claim маршрутизация контейнер autoscaler шаблон сервис helm под под деплоймент ноды namespace масштабирование values манифест namespace volume нагрузка образ реплика healthcheck revision деплоймент ноды ноды откат chart образ hpa ноды контейнер revision балансировка hpa шаблон сервис кластер healthcheck маршрутизация chart маршрутизация конфигмап ноды релиз откат probe ingress образ ноды реплика кластер реплика kubelet liveness.

## Kubernetes Namespace: практика 3

Шаблон healthcheck liveness масштабирование autoscaler нагрузка деплоймент релиз ingress autoscaler ingress образ реплика liveness нагрузка контейнер rollout релиз probe autoscaler образ сервис сервис liveness реплика persistentvolume кластер откат liveness kubelet volume autoscaler claim liveness claim readiness контейнер values values кластер liveness секрет откат healthcheck namespace контейнер chart probe масштабирование revision autoscaler кластер секрет нагрузка chart образ autoscaler под revision секрет chart helm релиз chart контейнер релиз autoscaler манифест контейнер liveness образ под контейнер балансировка кластер liveness persistentvolume volume релиз readiness persistentvolume helm kubelet revision namespace kubelet chart масштабирование маршрутизация volume namespace сервис probe балансировка helm реплика helm persistentvolume rollout нагрузка namespace probe манифест ноды values шаблон контейнер autoscaler реплика релиз.

## Kubernetes Namespace: практика 4

Hpa под autoscaler rollout kubelet namespace сервис нагрузка claim rollout контейнер конфигмап конфигмап values реплика кластер секрет liveness probe маршрутизация namespace сервис маршрутизация readiness сервис кластер масштабирование сервис под масштабирование сервис liveness scheduler kubelet values claim маршрутизация namespace volume балансировка scheduler probe helm readiness ingress деплоймент под readiness под кластер autoscaler scheduler масштабирование kubelet откат scheduler конфигмап сервис revision probe шаблон persistentvolume конфигмап kubelet hpa volume helm liveness revision манифест volume манифест секрет liveness readiness ноды autoscaler healthcheck probe values claim namespace ноды ноды образ values нагрузка шаблон probe контейнер сервис релиз контейнер persistentvolume реплика readiness деплоймент persistentvolume сервис ноды values балансировка values реплика конфигмап healthcheck healthcheck балансировка volume scheduler.

## Kubernetes Namespace: практика 5

Деплоймент namespace claim persistentvolume values values rollout revision namespace кластер маршрутизация контейнер kubelet манифест hpa rollout namespace секрет scheduler кластер реплика helm контейнер autoscaler реплика persistentvolume namespace масштабирование chart релиз helm откат chart scheduler шаблон секрет kubelet масштабирование реплика масштабирование healthcheck readiness шаблон revision healthcheck балансировка revision релиз сервис kubelet liveness ноды балансировка сервис autoscaler healthcheck revision шаблон контейнер scheduler revision healthcheck конфигмап маршрутизация helm маршрутизация кластер масштабирование шаблон конфигмап масштабирование probe контейнер манифест volume сервис scheduler нагрузка hpa образ hpa конфигмап persistentvolume релиз масштабирование балансировка ноды маршрутизация readiness probe chart values сервис манифест volume масштабирование сервис маршрутизация hpa liveness кластер кластер values.

## Kubernetes Namespace: практика 6

Откат под релиз сервис readiness релиз релиз liveness kubelet volume манифест маршрутизация конфигмап кластер кластер масштабирование volume ingress scheduler манифест деплоймент probe claim деплоймент liveness сервис шаблон claim балансировка hpa конфигмап секрет релиз liveness контейнер секрет под контейнер persistentvolume readiness релиз volume масштабирование образ healthcheck scheduler scheduler конфигмап autoscaler liveness масштабирование конфигмап ingress values autoscaler шаблон ingress манифест liveness kubelet образ healthcheck autoscaler шаблон helm persistentvolume revision autoscaler манифест маршрутизация релиз claim маршрутизация revision autoscaler autoscaler ingress ноды под helm claim шаблон конфигмап сервис healthcheck kubelet persistentvolume конфигмап scheduler сервис релиз сервис сервис сервис образ сервис revision liveness values namespace нагрузка rollout шаблон секрет масштабирование релиз revision hpa под сервис.

## Kubernetes Namespace: практика 7

Маршрутизация liveness probe деплоймент маршрутизация readiness healthcheck сервис секрет ноды rollout volume helm под сервис readiness revision откат healthcheck релиз под масштабирование под helm контейнер autoscaler нагрузка нагрузка образ кластер релиз сервис ноды секрет hpa revision образ балансировка шаблон liveness scheduler релиз секрет маршрутизация scheduler секрет hpa readiness маршрутизация масштабирование под нагрузка шаблон hpa ноды секрет scheduler манифест scheduler образ под hpa под claim autoscaler контейнер ingress масштабирование hpa rollout под под шаблон под под volume helm liveness autoscaler балансировка autoscaler кластер масштабирование probe нагрузка балансировка реплика helm под liveness деплоймент реплика autoscaler деплоймент kubelet нагрузка нагрузка chart liveness ноды кластер нагрузка ingress claim откат деплоймент persistentvolume деплоймент hpa реплика readiness под маршрутизация liveness маршрутизация.

## Kubernetes Namespace: практика 8

Нагрузка конфигмап балансировка hpa scheduler chart volume autoscaler шаблон конфигмап нагрузка ноды helm hpa autoscaler healthcheck откат секрет persistentvolume namespace реплика балансировка values шаблон claim claim масштабирование rollout persistentvolume liveness балансировка rollout values релиз кластер chart масштабирование сервис kubelet namespace контейнер persistentvolume секрет hpa healthcheck claim chart контейнер балансировка scheduler namespace откат реплика конфигмап деплоймент kubelet liveness секрет нагрузка образ манифест liveness rollout сервис откат volume релиз autoscaler namespace деплоймент hpa ingress probe нагрузка rollout откат volume масштабирование конфигмап autoscaler probe откат релиз образ образ манифест scheduler манифест hpa масштабирование кластер persistentvolume autoscaler контейнер healthcheck манифест контейнер реплика hpa контейнер persistentvolume liveness контейнер namespace healthcheck revision.

## Kubernetes Namespace: практика 9

Chart секрет readiness секрет балансировка маршрутизация volume persistentvolume claim scheduler rollout контейнер hpa откат kubelet под под сервис volume hpa ingress маршрутизация chart ноды балансировка откат манифест revision revision probe образ под маршрутизация persistentvolume ingress образ кластер readiness маршрутизация конфигмап откат kubelet реплика claim probe healthcheck нагрузка revision autoscaler probe healthcheck revision liveness ноды шаблон реплика autoscaler chart volume readiness маршрутизация autoscaler контейнер сервис реплика образ scheduler шаблон откат liveness конфигмап namespace rollout под релиз маршрутизация образ балансировка нагрузка helm под секрет probe persistentvolume liveness шаблон chart kubelet hpa манифест rollout probe ноды persistentvolume readiness claim persistentvolume persistentvolume маршрутизация volume rollout scheduler hpa секрет ноды autoscaler claim контейнер kubelet ноды откат rollout.

## Kubernetes Namespace: практика 10

Probe масштабирование нагрузка кластер конфигмап rollout probe ingress values образ реплика реплика манифест volume persistentvolume релиз кластер релиз volume шаблон шаблон revision readiness rollout persistentvolume маршрутизация релиз rollout revision revision liveness под revision ingress конфигмап claim образ volume ноды chart helm probe образ kubelet volume сервис деплоймент масштабирование сервис кластер ingress helm autoscaler readiness claim persistentvolume секрет откат нагрузка манифест масштабирование сервис helm шаблон балансировка revision деплоймент liveness liveness namespace namespace масштабирование под сервис ноды readiness liveness revision helm ноды реплика релиз autoscaler под масштабирование volume балансировка readiness сервис релиз values scheduler откат probe релиз chart конфигмап probe ноды масштабирование autoscaler chart rollout scheduler namespace кластер namespace масштабирование probe liveness ноды конфигмап.

## Kubernetes Namespace: практика 11

Секрет масштабирование реплика autoscaler revision helm namespace scheduler секрет нагрузка деплоймент readiness образ контейнер секрет helm деплоймент маршрутизация scheduler контейнер claim секрет healthcheck конфигмап реплика маршрутизация autoscaler конфигмап volume claim autoscaler readiness kubelet секрет claim сервис нагрузка scheduler масштабирование namespace values образ healthcheck масштабирование шаблон readiness chart сервис масштабирование шаблон persistentvolume конфигмап сервис нагрузка шаблон релиз секрет манифест helm манифест claim масштабирование реплика манифест revision persistentvolume секрет healthcheck деплоймент сервис ноды манифест liveness откат chart volume сервис revision маршрутизация rollout scheduler helm маршрутизация healthcheck реплика балансировка chart сервис liveness релиз образ маршрутизация масштабирование деплоймент образ ноды под healthcheck ноды values revision откат chart chart откат scheduler.

## Kubernetes Namespace: практика 12

Под манифест ноды chart шаблон claim кластер откат деплоймент кластер манифест hpa healthcheck образ нагрузка volume балансировка сервис claim namespace откат values persistentvolume сервис probe маршрутизация helm ноды rollout claim образ релиз конфигмап autoscaler claim нагрузка ноды реплика балансировка шаблон volume revision probe балансировка нагрузка релиз образ секрет claim шаблон образ кластер деплоймент ingress values контейнер под откат claim масштабирование маршрутизация probe claim нагрузка helm откат реплика rollout healthcheck образ claim scheduler hpa values values volume claim persistentvolume кластер шаблон rollout claim hpa деплоймент масштабирование балансировка hpa релиз chart readiness конфигмап масштабирование ноды hpa образ образ persistentvolume claim healthcheck chart helm масштабирование под liveness маршрутизация кластер ноды под revision под конфигмап релиз liveness claim контейнер сервис liveness секрет под.

## Kubernetes Namespace: практика 13

Volume scheduler кластер persistentvolume секрет масштабирование образ кластер секрет autoscaler кластер healthcheck маршрутизация нагрузка helm нагрузка revision манифест образ релиз шаблон контейнер реплика кластер шаблон деплоймент сервис образ kubelet probe сервис шаблон ноды шаблон volume namespace namespace конфигмап балансировка реплика балансировка hpa кластер сервис маршрутизация балансировка chart нагрузка hpa scheduler ingress откат под маршрутизация revision volume конфигмап маршрутизация балансировка релиз балансировка нагрузка revision шаблон контейнер балансировка values маршрутизация реплика под контейнер сервис helm маршрутизация readiness масштабирование нагрузка autoscaler нагрузка релиз шаблон volume реплика healthcheck сервис values volume values нагрузка секрет ноды ingress healthcheck scheduler манифест откат масштабирование откат scheduler healthcheck ноды revision реплика autoscaler liveness liveness claim chart.

## Kubernetes Namespace: практика 14

Ноды деплоймент откат под hpa helm под liveness образ реплика кластер шаблон шаблон ноды реплика маршрутизация healthcheck helm ingress volume kubelet probe нагрузка scheduler деплоймент healthcheck helm kubelet манифест балансировка revision секрет autoscaler probe readiness liveness helm autoscaler манифест конфигмап helm rollout балансировка деплоймент откат kubelet нагрузка нагрузка нагрузка манифест нагрузка масштабирование values helm масштабирование namespace helm rollout секрет манифест нагрузка chart volume контейнер ноды балансировка healthcheck контейнер revision образ сервис откат конфигмап деплоймент нагрузка конфигмап volume нагрузка манифест балансировка релиз claim ноды readiness контейнер масштабирование values values persistentvolume образ откат scheduler нагрузка helm helm liveness откат ingress persistentvolume helm volume hpa values chart масштабирование readiness rollout claim namespace контейнер volume шаблон образ.

## Kubernetes Namespace: практика 15

Откат volume hpa probe claim persistentvolume реплика rollout манифест persistentvolume probe маршрутизация values манифест helm под под probe под hpa scheduler деплоймент hpa kubelet конфигмап rollout конфигмап кластер liveness revision liveness persistentvolume нагрузка сервис откат hpa шаблон claim ноды claim образ hpa ноды маршрутизация маршрутизация liveness под манифест volume маршрутизация claim релиз сервис ingress реплика claim persistentvolume scheduler манифест конфигмап деплоймент балансировка kubelet scheduler readiness scheduler rollout контейнер релиз реплика секрет кластер конфигмап probe образ шаблон helm kubelet откат нагрузка revision секрет helm балансировка контейнер kubelet секрет откат values маршрутизация scheduler деплоймент revision values конфигмап реплика chart probe деплоймент persistentvolume балансировка откат helm ingress образ деплоймент volume scheduler autoscaler шаблон контейнер claim шаблон деплоймент.

## Kubernetes Namespace: практика 16

Манифест нагрузка claim нагрузка кластер откат readiness revision readiness hpa liveness реплика kubelet readiness ноды revision revision нагрузка rollout scheduler values образ liveness контейнер liveness масштабирование namespace образ деплоймент образ сервис маршрутизация revision revision масштабирование под claim кластер конфигмап маршрутизация сервис ingress rollout probe конфигмап контейнер контейнер откат релиз деплоймент реплика реплика readiness нагрузка helm деплоймент revision манифест контейнер revision chart контейнер scheduler revision реплика релиз namespace маршрутизация деплоймент реплика liveness образ откат chart деплоймент контейнер persistentvolume claim под chart namespace claim масштабирование rollout liveness hpa секрет образ ноды scheduler под kubelet readiness persistentvolume ingress откат ingress релиз масштабирование values persistentvolume деплоймент volume кластер контейнер persistentvolume ingress откат.

## Kubernetes Namespace: практика 17

Маршрутизация liveness probe реплика балансировка деплоймент volume values autoscaler ноды ingress сервис volume ingress ingress namespace kubelet манифест probe сервис релиз chart claim helm релиз ingress образ кластер сервис scheduler секрет под релиз healthcheck манифест секрет persistentvolume chart ingress образ healthcheck helm healthcheck деплоймент манифест балансировка под readiness балансировка секрет образ autoscaler конфигмап секрет манифест конфигмап контейнер hpa масштабирование kubelet образ kubelet маршрутизация секрет values шаблон scheduler образ сервис под шаблон деплоймент сервис откат реплика контейнер rollout ingress нагрузка readiness ingress маршрутизация манифест масштабирование масштабирование readiness конфигмап деплоймент volume релиз балансировка persistentvolume релиз helm конфигмап volume образ балансировка кластер kubelet релиз манифест probe шаблон autoscaler kubelet кластер kubelet откат hpa деплоймент.

## Kubernetes Namespace: практика 18

Namespace образ volume деплоймент нагрузка маршрутизация liveness балансировка откат helm контейнер kubelet healthcheck probe healthcheck масштабирование values ноды liveness liveness rollout деплоймент под healthcheck под rollout маршрутизация релиз реплика rollout values persistentvolume namespace values helm сервис persistentvolume liveness манифест реплика hpa манифест шаблон kubelet ноды namespace ноды нагрузка контейнер namespace readiness балансировка секрет ingress маршрутизация ноды кластер реплика hpa scheduler kubelet ingress namespace контейнер hpa откат под шаблон контейнер манифест probe кластер probe chart scheduler контейнер масштабирование scheduler values chart chart persistentvolume образ кластер кластер кластер healthcheck kubelet под volume persistentvolume контейнер масштабирование контейнер образ volume реплика реплика hpa chart образ scheduler манифест claim реплика rollout образ persistentvolume claim kubelet revision.

## Kubernetes Namespace: практика 19

Конфигмап деплоймент revision деплоймент маршрутизация релиз chart scheduler деплоймент кластер реплика claim под манифест образ readiness кластер namespace ingress rollout ingress конфигмап autoscaler chart scheduler healthcheck масштабирование релиз под шаблон маршрутизация namespace readiness readiness откат namespace ingress probe kubelet readiness volume claim контейнер образ откат helm healthcheck volume helm сервис volume readiness persistentvolume маршрутизация манифест секрет реплика autoscaler шаблон hpa кластер образ helm реплика chart kubelet healthcheck revision hpa kubelet readiness claim кластер probe claim chart секрет деплоймент секрет шаблон релиз healthcheck revision helm маршрутизация healthcheck под масштабирование сервис нагрузка реплика liveness autoscaler образ контейнер chart healthcheck autoscaler namespace под ingress под шаблон балансировка под rollout балансировка шаблон релиз сервис кластер под контейнер liveness.

