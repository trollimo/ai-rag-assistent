# Kubernetes: Pod — руководство 27

## Kubernetes Pod: практика 0

Namespace chart балансировка claim маршрутизация scheduler readiness конфигмап откат маршрутизация манифест манифест namespace kubelet ноды реплика persistentvolume манифест helm rollout сервис откат hpa readiness деплоймент revision манифест контейнер values секрет liveness healthcheck readiness values ingress манифест релиз реплика revision шаблон масштабирование ноды chart ноды балансировка values деплоймент healthcheck volume persistentvolume chart chart liveness helm конфигмап probe liveness kubelet chart реплика kubelet секрет autoscaler persistentvolume rollout балансировка liveness kubelet persistentvolume конфигмап ноды деплоймент persistentvolume hpa healthcheck values контейнер шаблон healthcheck rollout секрет масштабирование сервис откат конфигмап маршрутизация масштабирование helm scheduler autoscaler liveness scheduler ingress контейнер liveness секрет контейнер секрет кластер namespace сервис persistentvolume persistentvolume.

## Kubernetes Pod: практика 1

Claim сервис образ healthcheck конфигмап autoscaler ingress hpa масштабирование кластер ноды сервис namespace claim hpa масштабирование контейнер деплоймент healthcheck деплоймент rollout readiness маршрутизация liveness под namespace манифест scheduler rollout chart ingress persistentvolume кластер нагрузка релиз манифест реплика ноды реплика claim деплоймент сервис кластер probe scheduler под volume откат шаблон volume балансировка кластер hpa маршрутизация деплоймент конфигмап readiness namespace под маршрутизация шаблон манифест healthcheck сервис образ probe шаблон маршрутизация volume readiness claim маршрутизация ingress манифест шаблон образ helm конфигмап rollout реплика секрет scheduler релиз chart namespace readiness маршрутизация namespace реплика откат кластер chart деплоймент деплоймент масштабирование ноды liveness контейнер деплоймент autoscaler healthcheck autoscaler chart persistentvolume контейнер секрет конфигмап revision.

## Kubernetes Pod: практика 2

Claim helm сервис hpa сервис values сервис балансировка persistentvolume revision readiness под healthcheck helm релиз сервис шаблон persistentvolume контейнер volume откат под readiness scheduler релиз probe ingress hpa ноды маршрутизация откат helm scheduler hpa ingress helm namespace revision volume values revision hpa под liveness маршрутизация контейнер readiness autoscaler релиз persistentvolume масштабирование откат volume autoscaler конфигмап реплика манифест маршрутизация нагрузка readiness probe ноды под маршрутизация сервис hpa реплика kubelet kubelet healthcheck маршрутизация volume манифест readiness helm revision релиз откат persistentvolume ingress образ кластер readiness healthcheck конфигмап балансировка деплоймент под hpa helm под scheduler chart откат revision helm healthcheck сервис контейнер масштабирование релиз нагрузка volume readiness persistentvolume откат манифест values liveness шаблон volume chart релиз liveness.

## Kubernetes Pod: практика 3

Маршрутизация конфигмап масштабирование scheduler масштабирование rollout helm нагрузка нагрузка volume values volume контейнер манифест манифест readiness readiness релиз кластер под helm values масштабирование релиз healthcheck ingress values probe под volume масштабирование healthcheck healthcheck volume маршрутизация нагрузка манифест namespace нагрузка образ образ конфигмап под балансировка chart откат маршрутизация реплика helm values autoscaler probe chart hpa конфигмап claim chart persistentvolume образ реплика маршрутизация сервис секрет scheduler шаблон autoscaler helm кластер ноды scheduler откат helm scheduler volume rollout ноды конфигмап деплоймент probe autoscaler секрет ingress values конфигмап revision секрет секрет шаблон нагрузка ноды healthcheck healthcheck helm healthcheck секрет конфигмап балансировка autoscaler chart namespace rollout autoscaler масштабирование кластер сервис кластер scheduler кластер values шаблон.

## Kubernetes Pod: практика 4

Конфигмап манифест rollout образ namespace сервис volume релиз ingress ноды rollout манифест revision liveness liveness под persistentvolume релиз ingress деплоймент релиз образ kubelet claim откат healthcheck кластер persistentvolume сервис манифест kubelet probe откат контейнер liveness persistentvolume values persistentvolume сервис helm rollout шаблон релиз claim scheduler нагрузка chart values конфигмап секрет kubelet volume сервис volume autoscaler балансировка балансировка chart реплика конфигмап healthcheck кластер образ volume kubelet readiness scheduler claim kubelet namespace нагрузка откат hpa chart values конфигмап релиз сервис конфигмап ноды healthcheck ноды chart rollout балансировка claim нагрузка сервис revision values probe chart volume нагрузка реплика масштабирование values namespace namespace кластер rollout scheduler readiness релиз релиз readiness масштабирование kubelet масштабирование readiness helm маршрутизация.

## Kubernetes Pod: практика 5

Под liveness деплоймент autoscaler namespace сервис кластер релиз релиз ingress hpa балансировка кластер healthcheck namespace rollout liveness реплика ingress volume values hpa liveness откат контейнер persistentvolume откат деплоймент деплоймент конфигмап autoscaler секрет ingress ingress probe revision нагрузка ноды namespace конфигмап маршрутизация балансировка readiness конфигмап контейнер revision persistentvolume namespace нагрузка нагрузка масштабирование volume rollout chart liveness контейнер hpa ingress реплика шаблон persistentvolume readiness откат values namespace hpa ingress образ контейнер конфигмап манифест релиз кластер образ ingress балансировка autoscaler persistentvolume релиз liveness манифест кластер values сервис volume реплика rollout деплоймент под hpa kubelet kubelet readiness реплика откат маршрутизация persistentvolume readiness контейнер chart секрет откат образ ноды откат балансировка масштабирование нагрузка.

## Kubernetes Pod: практика 6

Values claim rollout откат autoscaler rollout healthcheck кластер namespace autoscaler scheduler revision масштабирование revision деплоймент манифест namespace балансировка шаблон revision ноды образ persistentvolume chart healthcheck образ кластер балансировка liveness балансировка values volume маршрутизация кластер откат chart ноды namespace ingress шаблон масштабирование autoscaler probe реплика секрет probe сервис kubelet нагрузка rollout откат healthcheck claim конфигмап rollout healthcheck образ реплика kubelet релиз scheduler ingress откат ноды откат rollout шаблон rollout persistentvolume кластер образ ingress kubelet kubelet rollout volume hpa scheduler liveness readiness масштабирование volume values маршрутизация конфигмап readiness volume chart chart образ values кластер деплоймент chart маршрутизация ноды контейнер шаблон autoscaler кластер ingress claim readiness revision маршрутизация helm кластер autoscaler helm readiness.

## Kubernetes Pod: практика 7

Масштабирование под chart readiness namespace liveness деплоймент hpa kubelet секрет autoscaler autoscaler контейнер liveness revision кластер ingress балансировка образ релиз scheduler volume конфигмап секрет ноды секрет kubelet ноды values ноды chart кластер helm hpa ingress chart балансировка маршрутизация манифест chart реплика revision манифест сервис реплика values healthcheck hpa манифест контейнер конфигмап autoscaler readiness chart healthcheck образ namespace манифест values helm volume readiness chart healthcheck probe persistentvolume под namespace манифест маршрутизация маршрутизация балансировка контейнер деплоймент values chart readiness под revision секрет probe откат под hpa секрет реплика probe revision namespace namespace values балансировка autoscaler маршрутизация helm scheduler kubelet конфигмап шаблон autoscaler балансировка kubelet hpa values namespace шаблон hpa под контейнер chart шаблон hpa helm шаблон kubelet.

## Kubernetes Pod: практика 8

Liveness helm кластер деплоймент конфигмап реплика revision healthcheck scheduler деплоймент revision claim scheduler namespace секрет autoscaler сервис rollout ingress деплоймент под кластер revision балансировка манифест нагрузка шаблон деплоймент healthcheck rollout volume масштабирование chart балансировка образ деплоймент образ hpa масштабирование деплоймент под probe нагрузка values балансировка балансировка healthcheck конфигмап readiness масштабирование шаблон образ namespace конфигмап откат ingress под rollout autoscaler chart конфигмап откат релиз балансировка volume шаблон healthcheck revision маршрутизация откат конфигмап namespace healthcheck rollout volume hpa релиз probe healthcheck образ балансировка chart маршрутизация rollout helm namespace реплика деплоймент claim readiness деплоймент namespace под revision шаблон readiness манифест шаблон конфигмап namespace контейнер балансировка конфигмап балансировка persistentvolume.

## Kubernetes Pod: практика 9

Образ секрет ноды chart сервис реплика ноды нагрузка kubelet hpa revision revision деплоймент hpa liveness liveness kubelet autoscaler scheduler namespace probe kubelet autoscaler readiness манифест scheduler маршрутизация контейнер конфигмап нагрузка healthcheck claim autoscaler namespace реплика namespace конфигмап деплоймент реплика контейнер масштабирование балансировка persistentvolume манифест chart реплика ноды persistentvolume сервис ingress probe scheduler hpa нагрузка секрет ingress секрет балансировка rollout rollout контейнер образ конфигмап scheduler контейнер claim откат scheduler контейнер claim образ rollout конфигмап volume namespace probe под релиз маршрутизация реплика revision probe chart контейнер масштабирование readiness hpa autoscaler readiness volume маршрутизация ноды values нагрузка нагрузка масштабирование релиз revision ingress values values helm секрет hpa реплика манифест деплоймент реплика chart probe probe.

## Kubernetes Pod: практика 10

Под контейнер балансировка шаблон chart секрет autoscaler маршрутизация helm volume контейнер kubelet манифест liveness конфигмап namespace claim probe реплика rollout масштабирование helm нагрузка rollout кластер образ релиз сервис ноды сервис конфигмап values volume откат values конфигмап ingress rollout балансировка конфигмап persistentvolume секрет readiness revision scheduler revision values helm values реплика откат контейнер chart образ volume hpa namespace rollout ingress claim rollout persistentvolume шаблон volume под kubelet маршрутизация балансировка claim autoscaler образ откат деплоймент релиз шаблон readiness helm шаблон chart деплоймент манифест chart масштабирование ноды persistentvolume volume кластер hpa volume реплика нагрузка маршрутизация под autoscaler контейнер claim helm autoscaler конфигмап healthcheck балансировка readiness claim деплоймент chart манифест откат кластер секрет деплоймент autoscaler helm claim сервис.

## Kubernetes Pod: практика 11

Релиз kubelet ingress нагрузка образ ingress релиз образ liveness rollout values откат контейнер под readiness readiness деплоймент scheduler реплика сервис контейнер hpa values ingress readiness шаблон конфигмап kubelet helm манифест сервис claim claim values шаблон liveness секрет helm сервис healthcheck секрет маршрутизация autoscaler ноды сервис манифест нагрузка релиз контейнер реплика образ маршрутизация секрет helm healthcheck volume autoscaler namespace autoscaler values volume образ образ манифест балансировка hpa probe откат под масштабирование healthcheck claim healthcheck масштабирование релиз маршрутизация values persistentvolume namespace hpa образ нагрузка kubelet конфигмап манифест ingress конфигмап маршрутизация autoscaler rollout конфигмап деплоймент ноды values откат revision healthcheck liveness манифест healthcheck scheduler helm балансировка под persistentvolume claim namespace claim liveness под релиз persistentvolume.

## Kubernetes Pod: практика 12

Helm values под revision values релиз откат hpa ingress claim hpa rollout балансировка ноды масштабирование probe revision revision values шаблон healthcheck chart ingress релиз под балансировка контейнер values контейнер реплика autoscaler сервис под values namespace деплоймент ingress конфигмап под healthcheck persistentvolume claim liveness autoscaler kubelet нагрузка деплоймент scheduler chart релиз rollout конфигмап revision values реплика volume chart конфигмап релиз probe volume persistentvolume контейнер rollout балансировка values hpa балансировка масштабирование autoscaler hpa сервис кластер namespace values liveness namespace секрет rollout autoscaler контейнер healthcheck values балансировка healthcheck ingress конфигмап ноды values persistentvolume volume revision балансировка autoscaler конфигмап kubelet масштабирование scheduler chart autoscaler readiness манифест реплика chart нагрузка helm масштабирование values балансировка.

## Kubernetes Pod: практика 13

Маршрутизация probe реплика конфигмап релиз шаблон маршрутизация probe revision контейнер нагрузка ноды persistentvolume volume hpa секрет манифест релиз claim hpa реплика нагрузка шаблон шаблон ingress контейнер rollout сервис rollout persistentvolume откат autoscaler контейнер секрет namespace релиз шаблон масштабирование масштабирование манифест probe values healthcheck откат образ релиз откат healthcheck namespace autoscaler деплоймент helm балансировка revision образ scheduler балансировка деплоймент revision scheduler сервис values chart persistentvolume релиз readiness helm шаблон probe реплика нагрузка секрет секрет persistentvolume кластер helm revision hpa ingress релиз ingress persistentvolume liveness liveness ingress hpa реплика readiness релиз маршрутизация балансировка helm kubelet scheduler claim маршрутизация autoscaler claim сервис релиз сервис под rollout манифест readiness откат секрет readiness откат claim scheduler.

## Kubernetes Pod: практика 14

Persistentvolume балансировка claim контейнер healthcheck kubelet helm values probe values scheduler revision helm claim values healthcheck ingress манифест сервис revision кластер шаблон реплика healthcheck реплика реплика volume масштабирование chart контейнер релиз нагрузка autoscaler кластер chart балансировка шаблон деплоймент реплика манифест кластер persistentvolume секрет healthcheck revision probe релиз revision helm кластер кластер healthcheck values секрет балансировка сервис кластер контейнер масштабирование шаблон конфигмап образ нагрузка rollout liveness откат под chart chart откат кластер откат probe helm claim values volume ноды нагрузка сервис секрет revision балансировка scheduler маршрутизация scheduler liveness namespace масштабирование autoscaler нагрузка scheduler probe реплика namespace chart kubelet деплоймент namespace scheduler ноды readiness values манифест балансировка ingress liveness readiness persistentvolume.

## Kubernetes Pod: практика 15

Откат деплоймент конфигмап релиз healthcheck probe манифест контейнер откат volume values values readiness ноды liveness сервис kubelet маршрутизация liveness кластер балансировка откат шаблон scheduler реплика кластер values образ образ liveness масштабирование манифест chart chart hpa деплоймент probe rollout healthcheck chart claim scheduler values манифест деплоймент сервис rollout liveness hpa сервис ноды балансировка ноды масштабирование probe readiness values volume ingress реплика persistentvolume liveness chart конфигмап конфигмап scheduler масштабирование volume под образ revision readiness релиз liveness нагрузка values кластер сервис ноды scheduler балансировка образ autoscaler readiness маршрутизация кластер autoscaler ноды values kubelet healthcheck readiness kubelet values ноды ноды values claim маршрутизация probe шаблон кластер под контейнер readiness claim claim под релиз под hpa откат ingress helm rollout values rollout.

## Kubernetes Pod: практика 16

Реплика сервис деплоймент liveness секрет балансировка autoscaler маршрутизация под probe под healthcheck probe rollout autoscaler кластер ingress масштабирование манифест claim scheduler деплоймент scheduler kubelet релиз образ нагрузка hpa под маршрутизация hpa контейнер балансировка нагрузка манифест конфигмап revision ноды kubelet namespace scheduler секрет namespace probe claim hpa kubelet кластер scheduler манифест кластер persistentvolume healthcheck реплика конфигмап chart autoscaler масштабирование helm helm манифест helm масштабирование релиз контейнер ingress манифест кластер деплоймент liveness кластер релиз hpa claim readiness readiness revision нагрузка readiness chart секрет hpa релиз реплика liveness контейнер секрет конфигмап liveness балансировка реплика маршрутизация kubelet откат ноды секрет балансировка scheduler liveness релиз probe ingress rollout деплоймент kubelet scheduler autoscaler образ healthcheck values ingress.

## Kubernetes Pod: практика 17

Образ контейнер namespace readiness probe probe кластер ноды образ реплика манифест нагрузка под контейнер шаблон kubelet конфигмап маршрутизация chart rollout нагрузка маршрутизация шаблон volume секрет манифест балансировка контейнер конфигмап сервис нагрузка релиз volume кластер манифест сервис балансировка контейнер нагрузка claim релиз контейнер релиз сервис namespace helm кластер volume релиз persistentvolume нагрузка volume секрет сервис readiness ноды секрет scheduler hpa values scheduler конфигмап секрет контейнер манифест scheduler балансировка rollout probe claim healthcheck балансировка реплика helm сервис revision helm под кластер сервис конфигмап шаблон манифест rollout rollout claim масштабирование revision readiness образ контейнер scheduler hpa ingress hpa маршрутизация values probe деплоймент реплика релиз конфигмап реплика healthcheck сервис сервис values kubelet релиз kubelet autoscaler реплика ноды autoscaler масштабирование.

## Kubernetes Pod: практика 18

Liveness rollout секрет autoscaler контейнер helm сервис балансировка liveness probe образ volume helm autoscaler healthcheck volume деплоймент rollout volume chart релиз манифест readiness реплика values liveness healthcheck helm values rollout под контейнер релиз namespace релиз helm нагрузка scheduler конфигмап конфигмап ingress под релиз маршрутизация chart балансировка под образ hpa ноды claim autoscaler ноды манифест helm values релиз hpa healthcheck масштабирование деплоймент values конфигмап нагрузка namespace деплоймент revision readiness revision кластер liveness секрет healthcheck values нагрузка секрет namespace релиз сервис volume scheduler деплоймент liveness helm namespace манифест откат values балансировка ingress liveness балансировка релиз probe кластер ingress liveness откат реплика values секрет chart образ образ volume ноды масштабирование откат ingress readiness балансировка healthcheck chart откат реплика кластер.

## Kubernetes Pod: практика 19

Healthcheck volume релиз ingress values манифест ноды helm под сервис секрет реплика hpa маршрутизация маршрутизация rollout helm образ балансировка балансировка деплоймент values chart liveness контейнер релиз секрет образ chart rollout scheduler контейнер healthcheck нагрузка конфигмап helm образ scheduler маршрутизация rollout liveness helm откат volume ingress маршрутизация claim ingress readiness балансировка autoscaler volume volume autoscaler ноды healthcheck шаблон ingress ноды образ балансировка probe rollout kubelet helm revision autoscaler readiness scheduler claim кластер сервис маршрутизация масштабирование реплика scheduler persistentvolume секрет нагрузка ingress сервис образ volume chart namespace кластер ingress volume ноды кластер probe hpa volume readiness сервис chart откат деплоймент ingress маршрутизация балансировка autoscaler масштабирование деплоймент под откат нагрузка chart hpa секрет манифест релиз readiness.

