# Kubernetes: Namespace — руководство 8

## Kubernetes Namespace: практика 0

Реплика нагрузка реплика readiness namespace kubelet kubelet контейнер scheduler values маршрутизация маршрутизация revision масштабирование сервис volume секрет релиз нагрузка под values revision образ шаблон реплика ingress контейнер claim кластер healthcheck readiness claim kubelet конфигмап деплоймент claim readiness persistentvolume claim revision kubelet манифест шаблон релиз probe сервис маршрутизация релиз liveness kubelet под ingress образ hpa реплика ingress rollout ingress образ chart контейнер scheduler hpa values сервис readiness кластер откат namespace healthcheck нагрузка kubelet масштабирование образ autoscaler под масштабирование сервис claim readiness сервис балансировка контейнер сервис volume реплика liveness namespace helm rollout образ scheduler readiness сервис шаблон реплика liveness values volume namespace реплика revision claim масштабирование откат healthcheck конфигмап helm кластер манифест ingress helm контейнер.

## Kubernetes Namespace: практика 1

Контейнер kubelet манифест persistentvolume autoscaler масштабирование persistentvolume readiness kubelet kubelet релиз healthcheck контейнер chart ingress hpa под volume helm helm кластер нагрузка readiness релиз kubelet кластер kubelet балансировка namespace persistentvolume деплоймент масштабирование шаблон маршрутизация probe контейнер образ реплика откат кластер scheduler kubelet сервис конфигмап volume контейнер деплоймент ingress реплика readiness healthcheck liveness ingress релиз образ контейнер релиз шаблон autoscaler hpa деплоймент откат под секрет шаблон деплоймент секрет манифест образ релиз rollout образ конфигмап healthcheck кластер масштабирование healthcheck values деплоймент helm liveness readiness ingress scheduler ноды hpa revision сервис scheduler ноды сервис балансировка chart healthcheck revision реплика healthcheck probe откат шаблон values манифест секрет kubelet манифест chart rollout revision ingress readiness.

## Kubernetes Namespace: практика 2

Под деплоймент релиз нагрузка kubelet маршрутизация контейнер chart liveness реплика rollout liveness autoscaler нагрузка сервис namespace scheduler реплика нагрузка контейнер релиз сервис autoscaler namespace шаблон claim rollout rollout readiness балансировка ingress namespace readiness контейнер autoscaler rollout revision rollout контейнер деплоймент autoscaler кластер релиз конфигмап autoscaler маршрутизация нагрузка ingress readiness persistentvolume масштабирование rollout values readiness rollout claim readiness под autoscaler манифест реплика контейнер релиз autoscaler образ scheduler probe секрет probe namespace манифест helm ноды namespace конфигмап кластер namespace namespace нагрузка rollout healthcheck откат нагрузка kubelet hpa rollout балансировка liveness кластер probe реплика откат образ persistentvolume values ingress релиз namespace kubelet ingress persistentvolume kubelet образ healthcheck hpa шаблон манифест healthcheck.

## Kubernetes Namespace: практика 3

Revision hpa hpa values volume ноды persistentvolume сервис liveness кластер манифест revision образ scheduler revision ноды ноды autoscaler шаблон релиз kubelet контейнер откат readiness rollout конфигмап балансировка деплоймент readiness под балансировка hpa volume сервис ноды балансировка нагрузка persistentvolume шаблон scheduler откат autoscaler autoscaler маршрутизация под rollout values откат autoscaler кластер autoscaler rollout values образ kubelet маршрутизация релиз кластер probe балансировка масштабирование readiness деплоймент kubelet контейнер деплоймент namespace конфигмап деплоймент chart ingress шаблон persistentvolume нагрузка chart scheduler сервис values chart probe scheduler под volume манифест шаблон образ релиз секрет hpa rollout сервис кластер volume контейнер нагрузка claim откат конфигмап healthcheck балансировка релиз ingress сервис kubelet секрет образ helm revision откат контейнер scheduler кластер деплоймент.

## Kubernetes Namespace: практика 4

Probe probe деплоймент масштабирование маршрутизация релиз autoscaler autoscaler образ autoscaler autoscaler под кластер liveness hpa балансировка сервис откат namespace readiness откат scheduler масштабирование сервис values секрет манифест кластер ноды релиз autoscaler kubelet autoscaler шаблон кластер hpa readiness конфигмап rollout ноды конфигмап namespace секрет volume релиз секрет реплика ingress ingress маршрутизация манифест реплика probe claim шаблон ноды образ persistentvolume ingress деплоймент probe namespace kubelet реплика values autoscaler hpa кластер chart scheduler секрет шаблон scheduler маршрутизация сервис rollout релиз секрет claim реплика ноды healthcheck под реплика контейнер конфигмап маршрутизация конфигмап масштабирование релиз манифест реплика revision ноды ноды откат hpa kubelet kubelet healthcheck scheduler реплика секрет kubelet релиз нагрузка под конфигмап секрет kubelet claim healthcheck readiness volume.

## Kubernetes Namespace: практика 5

Autoscaler контейнер readiness конфигмап секрет values helm rollout контейнер kubelet маршрутизация шаблон claim релиз кластер ноды маршрутизация манифест scheduler volume revision реплика сервис кластер volume persistentvolume values реплика под kubelet нагрузка манифест ingress манифест балансировка revision манифест readiness kubelet probe probe конфигмап кластер маршрутизация секрет деплоймент ноды контейнер сервис namespace манифест откат сервис namespace claim rollout релиз autoscaler масштабирование ноды реплика под autoscaler под сервис probe масштабирование кластер probe healthcheck hpa маршрутизация helm namespace манифест persistentvolume readiness под откат readiness маршрутизация scheduler релиз нагрузка kubelet деплоймент балансировка ingress нагрузка реплика chart масштабирование релиз values probe volume values kubelet persistentvolume ноды autoscaler реплика liveness hpa persistentvolume нагрузка claim релиз rollout манифест.

## Kubernetes Namespace: практика 6

Конфигмап маршрутизация ingress hpa values rollout секрет релиз claim откат реплика образ kubelet релиз конфигмап claim revision liveness volume балансировка helm деплоймент ingress маршрутизация helm балансировка readiness revision autoscaler healthcheck namespace readiness readiness кластер chart шаблон revision нагрузка healthcheck нагрузка контейнер helm hpa autoscaler ноды нагрузка деплоймент probe persistentvolume под autoscaler реплика деплоймент ноды шаблон образ релиз volume helm образ ноды values секрет autoscaler rollout балансировка откат values volume values конфигмап под образ сервис rollout деплоймент кластер readiness probe rollout revision rollout деплоймент kubelet persistentvolume манифест откат релиз релиз claim релиз релиз kubelet реплика autoscaler chart autoscaler claim деплоймент probe chart readiness hpa сервис под секрет шаблон ingress балансировка autoscaler liveness rollout rollout релиз autoscaler ноды kubelet.

## Kubernetes Namespace: практика 7

Масштабирование rollout балансировка реплика конфигмап секрет probe маршрутизация probe сервис откат readiness маршрутизация релиз ноды values образ namespace autoscaler ноды релиз манифест откат маршрутизация chart hpa values readiness нагрузка масштабирование шаблон шаблон сервис балансировка ingress hpa healthcheck контейнер кластер нагрузка chart манифест масштабирование rollout реплика hpa chart нагрузка балансировка ingress образ ingress под манифест values манифест под контейнер деплоймент масштабирование ноды liveness kubelet ноды ingress образ контейнер rollout hpa probe ноды ingress под hpa namespace revision helm claim ноды namespace revision реплика шаблон hpa revision секрет шаблон namespace rollout реплика probe откат scheduler hpa маршрутизация helm секрет конфигмап scheduler секрет сервис образ под helm контейнер liveness кластер манифест под claim откат volume ноды readiness деплоймент volume контейнер образ kubelet kubelet.

## Kubernetes Namespace: практика 8

Ноды kubelet откат namespace деплоймент под кластер claim конфигмап namespace namespace values revision под readiness autoscaler секрет сервис кластер откат probe образ масштабирование под claim hpa helm kubelet scheduler hpa helm revision hpa балансировка ноды autoscaler кластер нагрузка ноды liveness под масштабирование values autoscaler kubelet hpa hpa масштабирование ingress probe масштабирование healthcheck деплоймент claim контейнер claim kubelet манифест claim liveness деплоймент liveness ноды контейнер шаблон маршрутизация контейнер namespace маршрутизация kubelet persistentvolume liveness масштабирование hpa манифест ноды под конфигмап namespace hpa под claim маршрутизация деплоймент liveness claim volume rollout секрет ingress liveness healthcheck liveness revision namespace маршрутизация healthcheck revision ноды манифест деплоймент сервис autoscaler hpa балансировка откат namespace кластер маршрутизация kubelet откат манифест.

## Kubernetes Namespace: практика 9

Нагрузка балансировка probe volume rollout деплоймент readiness реплика релиз claim откат маршрутизация сервис hpa persistentvolume revision claim healthcheck образ секрет kubelet rollout маршрутизация под балансировка маршрутизация claim healthcheck под контейнер реплика деплоймент кластер values volume scheduler откат реплика liveness нагрузка chart маршрутизация откат probe манифест rollout нагрузка hpa секрет откат под реплика namespace healthcheck масштабирование масштабирование манифест нагрузка релиз readiness шаблон namespace маршрутизация scheduler деплоймент ноды autoscaler claim под probe деплоймент реплика persistentvolume hpa реплика namespace ingress claim под манифест liveness сервис namespace секрет манифест kubelet namespace образ claim values readiness реплика маршрутизация helm образ образ liveness деплоймент kubelet секрет под под probe релиз namespace rollout конфигмап hpa секрет revision values балансировка масштабирование.

## Kubernetes Namespace: практика 10

Liveness scheduler hpa chart scheduler persistentvolume маршрутизация релиз откат probe ingress scheduler namespace rollout балансировка probe helm конфигмап autoscaler namespace chart деплоймент образ релиз volume образ балансировка persistentvolume hpa кластер values манифест деплоймент liveness revision chart claim readiness релиз масштабирование шаблон volume балансировка scheduler масштабирование реплика кластер откат autoscaler kubelet образ kubelet readiness helm hpa values релиз конфигмап autoscaler rollout helm hpa конфигмап манифест секрет values ноды конфигмап кластер маршрутизация autoscaler реплика volume volume реплика readiness readiness откат реплика кластер claim readiness volume persistentvolume namespace claim кластер нагрузка chart манифест под образ autoscaler volume секрет сервис образ healthcheck конфигмап контейнер сервис values сервис namespace сервис liveness rollout ноды сервис контейнер балансировка volume revision.

## Kubernetes Namespace: практика 11

Readiness ноды реплика манифест нагрузка контейнер кластер claim values секрет откат namespace манифест под namespace маршрутизация манифест readiness секрет persistentvolume реплика под нагрузка scheduler реплика namespace секрет ingress volume chart манифест persistentvolume релиз readiness volume образ readiness revision образ healthcheck сервис readiness helm маршрутизация liveness hpa реплика persistentvolume claim контейнер values деплоймент реплика под кластер revision chart конфигмап под chart ingress под откат persistentvolume маршрутизация образ chart kubelet образ rollout revision деплоймент деплоймент autoscaler конфигмап сервис values volume манифест autoscaler persistentvolume persistentvolume шаблон claim volume балансировка образ claim ноды деплоймент образ реплика scheduler healthcheck под контейнер кластер kubelet hpa реплика деплоймент autoscaler ноды readiness probe релиз образ шаблон probe rollout откат маршрутизация.

## Kubernetes Namespace: практика 12

Chart helm revision конфигмап claim values контейнер под кластер healthcheck liveness ноды нагрузка volume секрет под chart нагрузка persistentvolume под деплоймент healthcheck chart ноды секрет релиз маршрутизация кластер манифест revision сервис liveness autoscaler релиз манифест реплика манифест балансировка values values конфигмап volume сервис деплоймент под образ образ scheduler образ persistentvolume шаблон под autoscaler hpa scheduler rollout релиз шаблон под балансировка ingress namespace ноды кластер readiness values маршрутизация под revision volume liveness volume hpa autoscaler readiness секрет манифест namespace нагрузка kubelet конфигмап нагрузка кластер образ values шаблон scheduler манифест под балансировка liveness claim persistentvolume масштабирование манифест деплоймент релиз liveness liveness контейнер маршрутизация под readiness ingress namespace persistentvolume нагрузка values hpa под манифест helm kubelet нагрузка.

## Kubernetes Namespace: практика 13

Сервис нагрузка деплоймент persistentvolume масштабирование ingress масштабирование healthcheck под claim сервис kubelet сервис ingress rollout kubelet chart autoscaler claim volume hpa hpa namespace секрет liveness probe нагрузка сервис ноды hpa масштабирование образ реплика volume шаблон probe autoscaler chart деплоймент hpa ноды балансировка rollout probe манифест rollout откат volume конфигмап реплика сервис rollout маршрутизация нагрузка probe нагрузка под реплика манифест hpa chart revision autoscaler маршрутизация values маршрутизация балансировка реплика релиз namespace откат откат healthcheck откат chart маршрутизация масштабирование ingress деплоймент scheduler claim helm под маршрутизация под маршрутизация образ chart реплика откат балансировка values маршрутизация маршрутизация readiness healthcheck persistentvolume маршрутизация сервис шаблон claim helm образ маршрутизация rollout namespace scheduler volume реплика scheduler.

## Kubernetes Namespace: практика 14

Релиз кластер helm под реплика persistentvolume values readiness liveness liveness persistentvolume кластер сервис autoscaler values маршрутизация деплоймент контейнер нагрузка namespace scheduler деплоймент probe конфигмап ноды образ нагрузка масштабирование chart ingress контейнер релиз шаблон autoscaler откат деплоймент ноды кластер нагрузка балансировка namespace helm ingress scheduler healthcheck под rollout реплика claim кластер hpa claim scheduler кластер liveness образ ноды values кластер балансировка namespace rollout кластер claim claim реплика конфигмап revision релиз конфигмап образ readiness kubelet кластер helm конфигмап helm values scheduler persistentvolume под конфигмап ноды нагрузка liveness helm маршрутизация нагрузка ноды маршрутизация kubelet ingress namespace revision балансировка релиз chart scheduler маршрутизация volume hpa секрет откат балансировка сервис rollout rollout образ autoscaler healthcheck сервис scheduler.

## Kubernetes Namespace: практика 15

Балансировка шаблон конфигмап namespace ноды релиз values helm hpa volume шаблон маршрутизация revision liveness манифест claim реплика claim autoscaler шаблон values шаблон релиз helm образ шаблон деплоймент namespace claim масштабирование healthcheck кластер контейнер claim откат claim liveness ноды откат healthcheck volume liveness кластер откат volume helm claim шаблон kubelet claim probe откат кластер ноды rollout ноды деплоймент kubelet chart откат маршрутизация namespace манифест volume revision claim контейнер autoscaler под volume конфигмап rollout namespace volume манифест кластер балансировка секрет claim ноды кластер конфигмап readiness манифест ingress шаблон под liveness откат маршрутизация шаблон claim ingress откат манифест ноды ingress revision реплика релиз autoscaler helm шаблон нагрузка scheduler ingress конфигмап секрет конфигмап chart реплика readiness scheduler values liveness values сервис контейнер контейнер autoscaler.

## Kubernetes Namespace: практика 16

Ingress healthcheck масштабирование persistentvolume нагрузка балансировка ingress namespace claim hpa readiness namespace hpa откат секрет нагрузка chart claim откат манифест kubelet rollout liveness релиз откат healthcheck реплика ingress healthcheck helm нагрузка контейнер масштабирование scheduler volume балансировка values readiness liveness readiness маршрутизация kubelet кластер секрет scheduler helm helm liveness под ingress откат реплика образ persistentvolume балансировка readiness autoscaler деплоймент контейнер claim секрет масштабирование образ образ контейнер балансировка probe манифест маршрутизация деплоймент конфигмап манифест секрет liveness helm ingress kubelet контейнер конфигмап values ingress chart кластер масштабирование namespace healthcheck нагрузка образ helm ingress helm ноды persistentvolume persistentvolume балансировка масштабирование кластер манифест контейнер манифест healthcheck шаблон persistentvolume маршрутизация.

## Kubernetes Namespace: практика 17

Claim persistentvolume chart шаблон балансировка нагрузка балансировка readiness values helm масштабирование манифест под revision claim probe values persistentvolume values маршрутизация values kubelet kubelet namespace rollout persistentvolume образ probe кластер namespace реплика hpa liveness образ rollout манифест ноды healthcheck ingress rollout нагрузка helm ноды образ сервис под healthcheck ingress балансировка кластер сервис persistentvolume hpa values hpa hpa манифест namespace healthcheck kubelet релиз autoscaler релиз кластер probe helm секрет readiness релиз маршрутизация балансировка chart chart балансировка балансировка нагрузка rollout контейнер сервис revision релиз балансировка persistentvolume hpa балансировка манифест манифест persistentvolume кластер реплика ноды манифест volume chart claim реплика ноды шаблон probe шаблон деплоймент ingress autoscaler реплика откат кластер healthcheck откат сервис шаблон под healthcheck.

## Kubernetes Namespace: практика 18

Секрет values hpa конфигмап ingress hpa секрет балансировка под деплоймент values ноды манифест кластер реплика rollout revision кластер values probe claim ноды деплоймент секрет откат probe образ claim секрет релиз деплоймент манифест деплоймент claim namespace kubelet откат деплоймент деплоймент volume volume kubelet откат контейнер namespace ноды chart секрет readiness hpa healthcheck persistentvolume манифест реплика probe под маршрутизация контейнер релиз нагрузка kubelet нагрузка под деплоймент healthcheck сервис маршрутизация namespace балансировка конфигмап деплоймент helm шаблон kubelet кластер liveness autoscaler liveness кластер образ масштабирование probe chart деплоймент нагрузка kubelet healthcheck probe values namespace манифест маршрутизация readiness ноды контейнер liveness volume масштабирование autoscaler probe autoscaler scheduler persistentvolume шаблон chart конфигмап helm ingress namespace ноды volume конфигмап helm.

## Kubernetes Namespace: практика 19

Контейнер rollout volume манифест values ingress балансировка образ нагрузка ingress деплоймент persistentvolume контейнер persistentvolume healthcheck ноды откат revision liveness revision probe под реплика под маршрутизация scheduler hpa liveness кластер конфигмап конфигмап autoscaler kubelet healthcheck кластер конфигмап readiness readiness конфигмап namespace scheduler helm ноды persistentvolume kubelet образ релиз volume образ контейнер scheduler шаблон persistentvolume секрет values probe helm образ ноды откат kubelet манифест liveness readiness namespace ingress persistentvolume маршрутизация ноды values секрет chart реплика claim liveness под манифест claim persistentvolume ingress нагрузка масштабирование нагрузка liveness scheduler chart kubelet конфигмап шаблон namespace секрет revision масштабирование hpa volume кластер persistentvolume revision scheduler kubelet шаблон конфигмап volume релиз revision values конфигмап релиз.

