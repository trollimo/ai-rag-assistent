# Kubernetes: Pod — руководство 1

## Kubernetes Pod: практика 0

Образ scheduler контейнер шаблон chart деплоймент сервис образ readiness реплика шаблон образ кластер readiness values autoscaler chart реплика маршрутизация rollout revision values секрет секрет шаблон конфигмап балансировка масштабирование values scheduler конфигмап откат реплика hpa манифест kubelet hpa healthcheck реплика persistentvolume ноды масштабирование секрет scheduler scheduler балансировка конфигмап ноды манифест autoscaler probe шаблон values конфигмап балансировка liveness реплика hpa scheduler scheduler hpa масштабирование маршрутизация ноды шаблон релиз chart volume namespace readiness claim namespace autoscaler helm реплика откат ноды образ деплоймент helm hpa chart probe chart volume конфигмап под chart helm ingress probe шаблон chart кластер шаблон hpa секрет revision ноды liveness autoscaler persistentvolume конфигмап релиз claim rollout healthcheck liveness сервис probe секрет chart claim секрет scheduler образ values.

## Kubernetes Pod: практика 1

Балансировка контейнер релиз hpa scheduler revision балансировка helm volume values ноды шаблон healthcheck hpa volume релиз healthcheck healthcheck liveness нагрузка persistentvolume нагрузка маршрутизация autoscaler healthcheck манифест autoscaler реплика масштабирование образ деплоймент реплика под scheduler маршрутизация values деплоймент балансировка под chart rollout liveness claim реплика probe healthcheck контейнер под autoscaler hpa scheduler под контейнер масштабирование откат autoscaler revision контейнер rollout kubelet манифест сервис сервис масштабирование балансировка шаблон helm масштабирование релиз persistentvolume откат кластер namespace claim кластер liveness шаблон values конфигмап namespace образ namespace кластер ноды ноды hpa probe реплика под нагрузка namespace healthcheck kubelet volume сервис масштабирование образ values chart probe откат образ деплоймент откат autoscaler образ манифест секрет контейнер chart.

## Kubernetes Pod: практика 2

Балансировка кластер деплоймент конфигмап нагрузка шаблон ноды autoscaler volume ноды маршрутизация namespace rollout persistentvolume реплика сервис kubelet values деплоймент volume кластер секрет контейнер откат readiness healthcheck values persistentvolume hpa ноды kubelet манифест ноды namespace revision шаблон деплоймент namespace откат балансировка ноды балансировка ноды секрет claim сервис namespace под конфигмап readiness claim откат сервис маршрутизация namespace манифест kubelet readiness hpa hpa rollout scheduler сервис контейнер namespace persistentvolume hpa namespace секрет ноды под revision scheduler ноды namespace hpa шаблон масштабирование нагрузка ноды нагрузка маршрутизация откат конфигмап откат healthcheck балансировка деплоймент нагрузка kubelet откат scheduler конфигмап probe деплоймент claim релиз шаблон healthcheck helm liveness сервис маршрутизация конфигмап под кластер volume ноды volume readiness volume kubelet.

## Kubernetes Pod: практика 3

Маршрутизация конфигмап секрет helm probe секрет манифест кластер секрет revision healthcheck шаблон секрет ноды откат кластер claim ingress helm autoscaler манифест hpa контейнер liveness chart readiness revision деплоймент scheduler healthcheck маршрутизация конфигмап шаблон релиз балансировка kubelet ноды деплоймент readiness probe кластер autoscaler rollout scheduler autoscaler образ claim autoscaler namespace сервис шаблон persistentvolume нагрузка образ кластер volume kubelet сервис под сервис релиз values rollout масштабирование hpa values readiness claim кластер liveness rollout балансировка persistentvolume контейнер probe сервис revision scheduler namespace ingress балансировка persistentvolume конфигмап volume liveness chart масштабирование probe persistentvolume probe сервис readiness readiness секрет деплоймент конфигмап образ ingress контейнер volume сервис нагрузка шаблон масштабирование манифест hpa нагрузка volume hpa kubelet.

## Kubernetes Pod: практика 4

Liveness values кластер манифест манифест kubelet кластер контейнер helm readiness kubelet ноды под chart chart откат деплоймент сервис autoscaler манифест claim секрет helm liveness ingress релиз healthcheck масштабирование секрет ноды кластер ingress persistentvolume persistentvolume liveness манифест конфигмап namespace маршрутизация секрет ingress шаблон конфигмап ingress манифест claim балансировка rollout ноды scheduler маршрутизация ноды kubelet манифест healthcheck autoscaler манифест hpa нагрузка healthcheck revision маршрутизация манифест секрет readiness реплика persistentvolume под healthcheck маршрутизация helm ноды hpa rollout healthcheck scheduler healthcheck scheduler readiness healthcheck нагрузка namespace контейнер values revision образ откат values autoscaler revision нагрузка liveness healthcheck hpa rollout ingress ingress откат autoscaler claim autoscaler образ readiness autoscaler chart kubelet сервис шаблон конфигмап.

## Kubernetes Pod: практика 5

Образ readiness релиз persistentvolume namespace readiness ingress шаблон chart persistentvolume kubelet нагрузка маршрутизация chart chart реплика persistentvolume autoscaler откат autoscaler нагрузка секрет revision revision откат rollout сервис маршрутизация scheduler реплика откат маршрутизация маршрутизация шаблон балансировка секрет шаблон кластер healthcheck балансировка откат scheduler ноды readiness деплоймент namespace кластер rollout откат claim liveness healthcheck секрет под namespace liveness persistentvolume деплоймент autoscaler кластер chart kubelet chart healthcheck откат секрет revision volume hpa маршрутизация healthcheck volume реплика образ сервис liveness контейнер ingress нагрузка readiness liveness под kubelet helm маршрутизация конфигмап нагрузка откат namespace claim релиз namespace claim нагрузка реплика кластер конфигмап values kubelet namespace сервис под chart probe деплоймент маршрутизация values масштабирование.

## Kubernetes Pod: практика 6

Readiness под под volume ingress kubelet маршрутизация probe деплоймент values ноды namespace сервис сервис секрет claim конфигмап маршрутизация ноды маршрутизация балансировка деплоймент масштабирование под нагрузка readiness persistentvolume chart балансировка scheduler readiness chart namespace сервис маршрутизация ingress ноды persistentvolume kubelet секрет маршрутизация шаблон откат namespace шаблон hpa revision контейнер конфигмап chart масштабирование rollout helm кластер revision реплика шаблон healthcheck ноды ingress шаблон healthcheck деплоймент масштабирование под claim секрет маршрутизация ноды scheduler claim autoscaler манифест probe деплоймент контейнер деплоймент под релиз chart образ реплика кластер rollout revision rollout scheduler kubelet кластер namespace autoscaler autoscaler балансировка откат namespace конфигмап namespace rollout chart балансировка autoscaler секрет кластер деплоймент конфигмап kubelet шаблон реплика.

## Kubernetes Pod: практика 7

Шаблон namespace kubelet ingress liveness реплика конфигмап манифест ingress под маршрутизация под нагрузка healthcheck persistentvolume масштабирование масштабирование релиз нагрузка claim маршрутизация откат ingress контейнер volume kubelet откат масштабирование деплоймент релиз деплоймент healthcheck liveness rollout claim реплика healthcheck нагрузка claim нагрузка релиз сервис ingress values rollout под образ под шаблон образ probe claim ноды балансировка манифест образ hpa scheduler values секрет шаблон scheduler секрет откат деплоймент claim ingress rollout реплика манифест values масштабирование образ кластер деплоймент liveness revision конфигмап секрет namespace hpa liveness под claim helm revision конфигмап шаблон liveness claim healthcheck helm autoscaler шаблон балансировка под scheduler контейнер values autoscaler persistentvolume под volume под ноды конфигмап сервис chart масштабирование шаблон scheduler healthcheck реплика.

## Kubernetes Pod: практика 8

Балансировка откат namespace образ values масштабирование scheduler claim liveness namespace сервис rollout маршрутизация кластер деплоймент реплика kubelet маршрутизация шаблон revision релиз секрет кластер секрет секрет readiness откат values деплоймент helm маршрутизация сервис балансировка сервис нагрузка readiness liveness секрет scheduler ingress autoscaler persistentvolume chart readiness конфигмап liveness релиз ingress namespace нагрузка конфигмап образ chart секрет балансировка namespace шаблон persistentvolume кластер rollout реплика конфигмап релиз манифест контейнер кластер readiness hpa hpa шаблон сервис liveness сервис маршрутизация ingress под реплика values маршрутизация helm кластер claim namespace релиз деплоймент контейнер деплоймент helm readiness маршрутизация namespace volume ingress образ liveness реплика нагрузка kubelet кластер healthcheck контейнер ноды kubelet манифест persistentvolume chart манифест откат откат.

## Kubernetes Pod: практика 9

Readiness балансировка образ шаблон revision healthcheck scheduler hpa probe реплика namespace откат ноды маршрутизация scheduler helm масштабирование шаблон autoscaler контейнер ноды persistentvolume probe autoscaler healthcheck persistentvolume rollout секрет ноды откат манифест контейнер секрет values контейнер откат реплика probe масштабирование persistentvolume values нагрузка сервис шаблон шаблон откат autoscaler реплика values ноды persistentvolume манифест манифест шаблон секрет persistentvolume контейнер persistentvolume балансировка helm healthcheck autoscaler конфигмап манифест healthcheck образ namespace реплика values values контейнер масштабирование probe под volume helm маршрутизация volume манифест секрет реплика контейнер балансировка балансировка ingress масштабирование claim деплоймент claim кластер сервис volume конфигмап секрет маршрутизация нагрузка kubelet healthcheck секрет манифест hpa persistentvolume values манифест.

## Kubernetes Pod: практика 10

Ноды конфигмап revision маршрутизация манифест под кластер образ манифест секрет values сервис healthcheck балансировка volume деплоймент шаблон persistentvolume секрет нагрузка namespace volume readiness под релиз chart шаблон деплоймент persistentvolume релиз сервис под реплика ноды readiness scheduler нагрузка scheduler healthcheck ноды persistentvolume autoscaler ноды сервис probe values нагрузка readiness деплоймент образ revision контейнер секрет релиз сервис деплоймент volume нагрузка ноды нагрузка деплоймент hpa namespace volume probe контейнер kubelet нагрузка kubelet нагрузка реплика сервис volume chart helm scheduler hpa autoscaler релиз revision конфигмап шаблон сервис реплика кластер hpa healthcheck балансировка реплика claim под kubelet namespace revision probe helm сервис контейнер chart нагрузка revision namespace probe ingress конфигмап кластер probe healthcheck релиз namespace autoscaler ноды манифест claim ноды claim.

## Kubernetes Pod: практика 11

Манифест readiness образ релиз readiness liveness релиз values откат реплика балансировка сервис namespace persistentvolume autoscaler scheduler values hpa реплика ноды масштабирование scheduler autoscaler probe шаблон probe конфигмап namespace кластер образ rollout масштабирование балансировка ingress readiness манифест нагрузка chart helm маршрутизация образ kubelet namespace kubelet helm балансировка релиз секрет деплоймент namespace ноды hpa кластер hpa секрет откат масштабирование liveness под масштабирование scheduler ingress scheduler балансировка балансировка сервис сервис сервис revision autoscaler persistentvolume scheduler релиз ноды healthcheck probe образ манифест откат кластер балансировка кластер образ кластер volume шаблон реплика probe откат claim контейнер volume конфигмап конфигмап откат healthcheck сервис values маршрутизация конфигмап readiness секрет откат scheduler namespace helm readiness chart scheduler healthcheck.

## Kubernetes Pod: практика 12

Масштабирование claim реплика rollout нагрузка деплоймент конфигмап rollout кластер маршрутизация масштабирование контейнер манифест helm helm реплика шаблон образ liveness реплика claim helm откат claim liveness rollout кластер ingress контейнер шаблон claim healthcheck readiness манифест probe hpa persistentvolume persistentvolume нагрузка релиз namespace revision контейнер ноды реплика нагрузка chart values values chart откат scheduler rollout контейнер под probe маршрутизация манифест под values релиз конфигмап откат kubelet ноды persistentvolume liveness релиз chart liveness конфигмап масштабирование кластер сервис под rollout claim hpa revision конфигмап сервис probe деплоймент volume откат секрет откат релиз конфигмап helm балансировка масштабирование шаблон реплика кластер секрет маршрутизация контейнер healthcheck revision релиз scheduler кластер ноды образ helm volume values autoscaler probe persistentvolume rollout конфигмап.

## Kubernetes Pod: практика 13

Нагрузка hpa манифест revision сервис probe маршрутизация релиз hpa манифест volume namespace values persistentvolume values конфигмап scheduler volume claim релиз revision liveness ingress probe реплика деплоймент probe helm rollout ingress ноды масштабирование конфигмап scheduler реплика балансировка маршрутизация helm volume scheduler liveness балансировка секрет манифест namespace healthcheck volume манифест секрет реплика под readiness откат chart нагрузка нагрузка healthcheck namespace маршрутизация kubelet kubelet кластер kubelet probe ноды под образ autoscaler helm claim под секрет масштабирование helm контейнер под шаблон values persistentvolume конфигмап маршрутизация сервис ноды rollout конфигмап нагрузка деплоймент образ конфигмап revision liveness масштабирование масштабирование volume kubelet маршрутизация volume healthcheck persistentvolume helm нагрузка кластер контейнер values values маршрутизация chart контейнер rollout.

## Kubernetes Pod: практика 14

Нагрузка ingress claim образ ноды релиз autoscaler реплика реплика сервис контейнер реплика ingress scheduler readiness ноды ingress hpa релиз probe балансировка chart под конфигмап scheduler сервис масштабирование откат маршрутизация scheduler сервис rollout ingress revision ingress сервис healthcheck helm helm релиз масштабирование helm persistentvolume scheduler revision нагрузка образ hpa healthcheck chart probe volume шаблон под под namespace ingress healthcheck балансировка namespace rollout деплоймент сервис релиз values liveness revision релиз liveness реплика сервис манифест образ revision claim секрет kubelet ingress сервис образ под маршрутизация нагрузка volume persistentvolume readiness откат namespace claim volume манифест ingress кластер кластер liveness откат ноды probe scheduler нагрузка ingress маршрутизация реплика нагрузка сервис scheduler probe readiness манифест ноды persistentvolume контейнер маршрутизация probe healthcheck.

## Kubernetes Pod: практика 15

Ingress scheduler readiness образ деплоймент релиз секрет под масштабирование values volume масштабирование масштабирование сервис kubelet шаблон probe persistentvolume ноды балансировка hpa реплика под нагрузка rollout probe healthcheck нагрузка деплоймент healthcheck секрет шаблон volume откат реплика rollout values hpa ноды кластер шаблон namespace liveness readiness readiness namespace healthcheck readiness секрет ingress healthcheck контейнер сервис кластер scheduler образ hpa hpa под namespace конфигмап rollout hpa chart liveness volume claim реплика values liveness volume hpa probe claim readiness ingress сервис values под volume persistentvolume revision hpa масштабирование claim сервис образ нагрузка healthcheck маршрутизация scheduler сервис релиз helm helm ingress кластер chart liveness helm volume сервис scheduler образ ingress readiness liveness rollout маршрутизация healthcheck под масштабирование реплика hpa kubelet probe.

## Kubernetes Pod: практика 16

Rollout манифест манифест маршрутизация сервис ноды секрет rollout ноды scheduler шаблон откат кластер volume revision readiness маршрутизация маршрутизация scheduler rollout конфигмап hpa секрет hpa chart helm chart revision persistentvolume маршрутизация манифест rollout autoscaler секрет ingress persistentvolume hpa probe scheduler autoscaler revision kubelet под масштабирование сервис контейнер ноды балансировка реплика реплика helm откат нагрузка revision релиз балансировка реплика revision chart autoscaler образ persistentvolume readiness сервис hpa values hpa ноды шаблон rollout revision реплика ноды ноды scheduler секрет hpa реплика под релиз helm readiness values claim autoscaler helm helm revision readiness манифест деплоймент манифест манифест rollout ingress ноды образ kubelet откат hpa релиз маршрутизация нагрузка релиз под volume liveness балансировка образ балансировка секрет реплика конфигмап healthcheck hpa persistentvolume.

## Kubernetes Pod: практика 17

Деплоймент секрет масштабирование values манифест масштабирование деплоймент rollout нагрузка scheduler autoscaler autoscaler кластер namespace persistentvolume values chart volume volume claim revision chart масштабирование probe hpa под ingress persistentvolume readiness шаблон шаблон образ секрет ноды ingress ноды релиз autoscaler revision контейнер масштабирование claim rollout rollout readiness healthcheck образ revision helm секрет rollout namespace сервис нагрузка autoscaler autoscaler под деплоймент chart chart kubelet сервис chart autoscaler kubelet деплоймент балансировка контейнер liveness volume образ namespace деплоймент реплика манифест релиз hpa деплоймент values релиз scheduler ноды readiness кластер kubelet секрет контейнер ноды ingress autoscaler под деплоймент образ деплоймент persistentvolume rollout сервис revision конфигмап namespace revision readiness ноды кластер контейнер probe нагрузка autoscaler values маршрутизация.

## Kubernetes Pod: практика 18

Кластер autoscaler revision балансировка конфигмап claim под hpa hpa балансировка ingress образ под кластер autoscaler hpa namespace конфигмап chart persistentvolume chart scheduler rollout балансировка нагрузка манифест values релиз helm ingress манифест реплика hpa liveness hpa деплоймент revision деплоймент релиз сервис helm healthcheck volume persistentvolume маршрутизация шаблон сервис revision деплоймент helm секрет healthcheck readiness volume конфигмап балансировка claim секрет kubelet нагрузка под секрет шаблон values kubelet откат claim маршрутизация volume probe liveness readiness persistentvolume конфигмап healthcheck hpa сервис манифест namespace откат маршрутизация autoscaler helm релиз деплоймент деплоймент контейнер rollout контейнер кластер chart образ hpa релиз autoscaler autoscaler probe балансировка claim кластер values revision маршрутизация liveness rollout rollout namespace кластер claim rollout под шаблон нагрузка.

## Kubernetes Pod: практика 19

Rollout маршрутизация readiness нагрузка манифест балансировка probe persistentvolume образ values healthcheck values деплоймент релиз claim values образ chart масштабирование claim ноды probe liveness образ образ секрет сервис масштабирование откат релиз autoscaler манифест ноды ноды claim деплоймент масштабирование релиз ноды values масштабирование кластер persistentvolume persistentvolume healthcheck probe под rollout маршрутизация readiness нагрузка values деплоймент rollout откат chart балансировка манифест нагрузка claim ingress values scheduler volume ingress probe probe probe chart нагрузка scheduler контейнер шаблон образ маршрутизация нагрузка маршрутизация rollout образ балансировка реплика нагрузка релиз манифест масштабирование кластер контейнер revision namespace chart readiness probe probe нагрузка values scheduler балансировка манифест rollout релиз нагрузка volume liveness деплоймент values ноды ноды маршрутизация сервис.

